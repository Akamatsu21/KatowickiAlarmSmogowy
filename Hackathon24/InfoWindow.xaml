<Window x:Class="KatowickiAlarmSmogowy.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KatowickiAlarmSmogowy"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Informacje" Height="180" Width="300" Icon="images\icon.ico">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Helvetica" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Margin" Value="5, 10, 0, 0" />
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Name="Title" FontWeight="Bold" />
            <TextBlock Name="Current" />
            <TextBlock Name="Norm" />
            <TextBlock Name="Diff" />
        </StackPanel>
    </Grid>
</Window>
